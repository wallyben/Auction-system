# Backtest report

No owner-recorded sales were present at completion. The backtest runner executed and correctly refused to invent a win rate.

`make backtest` writes `artifacts/backtest_results.json`.

A lookahead-free BUY_READY simulation requires dated opportunity snapshots plus later observed outcomes. Those do not exist in this repository yet.
